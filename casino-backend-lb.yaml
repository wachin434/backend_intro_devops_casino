apiVersion: v1
kind: Service
metadata:
  name: casino-backend-lb
  labels:
    app: casino-backend
spec:
  type: LoadBalancer          
  selector:
    app: casino-backend       
  ports:
    - name: http
      protocol: TCP
      port: 80                
      targetPort: 3000